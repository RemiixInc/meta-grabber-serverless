{
  "name": "meta-grabber-serverless",
  "version": "1.0.0",
  "description": "Meta Grabber API built using serverless functions.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "meta-grabber": "^1.0.0"
  },
  "engines": {
    "node": "12.x"
  }
}